package c.x.codingtest.easy

object ProgrammersSample extends App {
//  val a = "aaaaa"
//  val b = "bbbbb"
//
//  println(a.map(_.toString).zip(b.map(_.toString)).map(a => {a._1 + a._2}).mkString)

//  def solution(my_string: String, overwrite_string: String, s: Int): String = {
//    val modLen = s + overwrite_string.length
//    val tail = if (my_string.length > modLen) my_string.substring(modLen) else ""
//    my_string.substring(0, s) + overwrite_string + tail
//  }
//  println(solution("He11oWor1d", "lloWorl", 2))

//  val v = "abcde"
//  v.foreach(println)

//  val s1 = "aBcD"
//  println(s1.map(_.toString)
//    .map(c => if(('a' to 'z').mkString.contains(c)) c.toUpperCase()
//              else c.toLowerCase())
//    .mkString)
//  println(('a' to 'z').mkString)


  println("abc" * 3)

}
